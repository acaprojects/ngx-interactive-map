import { MapListenerFeature } from './map-listener-feature';

describe('MapListenerFeature', () => {
  it('should create an instance', () => {
    expect(new MapListenerFeature()).toBeTruthy();
  });
});
