import { MapZoomDirective } from './map-zoom.directive';

describe('MapZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new MapZoomDirective();
    expect(directive).toBeTruthy();
  });
});
